# Optional environment variables (local / advanced deploys)

# Writable folder for SQLite (default: app directory, or home on Streamlit Cloud)
# TRACKER_DATA_DIR=

# Pro unlock (also configurable via Streamlit secrets)
PRO_UNLOCK_CODES=STUDYPRO,SHOWUP2026
PRO_PRICE_INR=499
PRO_PAYMENT_LINK=
PRO_SUPPORT_EMAIL=support@example.com

# Optional Reflex / Postgres (not required for Streamlit demo)
# DATABASE_URL=sqlite:///study_routine_tracker.db
