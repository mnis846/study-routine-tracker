{
  "name": "study-tracker-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android shell for the offline Study Tracker tablet app",
  "scripts": {
    "sync-web": "node scripts/sync-web.js",
    "cap:sync": "npm run sync-web && npx cap sync android",
    "build:android": "npm run cap:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
